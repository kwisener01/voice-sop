[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["mkdir -p credentials", "mkdir -p instance"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:${PORT:-5000} --workers 4 --timeout 120"
